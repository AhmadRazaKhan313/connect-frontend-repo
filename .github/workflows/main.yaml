# name: Build and Deploy

# on:
#     push:
#         branches:
#             - main

# jobs:
#     build-and-deploy:
#         runs-on: ubuntu-latest

#         steps:
#             - name: Checkout repository
#               uses: actions/checkout@v2

#             - name: Install Node.js
#               uses: actions/setup-node@v2
#               with:
#                   node-version: '14'

#             - name: Install dependencies
#               run: npm install

#             - name: Build
#               run: npm run build

#             - name: Deploy
#               uses: SamKirkland/FTP-Deploy-Action@4.2.0
#               with:
#                   server: ${{ secrets.FTP_SERVER }}
#                   username: ${{ secrets.FTP_USERNAME }}
#                   password: ${{ secrets.FTP_PASSWORD }}
#                   protocol: ftps
#                   port: ${{ secrets.FTP_PORT }}
#                   local-dir: ./build/
#                   server-dir: /public_html/
#     notify:
#         runs-on: ubuntu-latest
#         needs: build-and-deploy
#         if: always()
#         steps:
#             - name: Send email notification
#               uses: dawidd6/action-send-mail@v3.3.0
#               with:
#                   server_address: ${{ secrets.MY_EMAIL_SERVER }}
#                   server_port: ${{ secrets.MY_EMAIL_PORT }}
#                   username: ${{ secrets.MY_EMAIL }}
#                   password: ${{ secrets.MY_EMAIL_PASSWORD }}
#                   subject: 'Github Action succeeded'
#                   body: 'Your Github Pipeline Action workflow has succeeded!'
#                   to: 'haseebbhatti742@gmail.com'
#                   from: 'Github Pipeline <info@connectcommunicationslodhran.com>'
